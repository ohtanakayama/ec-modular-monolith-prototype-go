version: v2
modules:
  - path: .
lint:
  use:
    - STANDARD
  except:
    # Allow leading numeric components in service names if needed in future.
    - PACKAGE_VERSION_SUFFIX
breaking:
  use:
    - FILE
